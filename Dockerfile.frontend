# Frontend Dockerfile
FROM node:20-alpine

WORKDIR /app

# 複製 package.json
COPY package.json package-lock.json* ./

# 安裝依賴
RUN npm ci

# 複製應用代碼
COPY . .

# 暴露端口
EXPOSE 5173

# 啟動開發服務器
CMD ["npm", "run", "dev"]
